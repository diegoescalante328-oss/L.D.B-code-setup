Implement Version 1 of Screen Analyzer.

Requirements:
- external camera source
- automatic timed capture
- one in-flight analysis request max
- latest-frame-wins queue
- local UI
- structured JSON output
- visible error state
- traceable logs

Implement:
1. camera ingestion
2. smoke capture
3. single-frame analysis
4. bounded queue policy
5. desktop UI shell
6. coordinator loop
7. logs
8. tests

Do not add non-V1 features.
