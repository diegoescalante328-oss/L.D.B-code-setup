# Deprecated root-level implementation snapshot

This folder contains archived duplicates that previously lived at repository top-level (`001_app`, `002_config`, etc.).

Use `screen_analyzer_v1/` for all active development, runtime execution, tests, and docs.
